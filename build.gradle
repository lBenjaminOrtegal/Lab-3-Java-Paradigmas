plugins {
    id 'application'
}

repositories {
    mavenCentral()
}

application {
    mainClass = 'Main' // Asegúrate de que la clase Main exista en esa ruta
}

run {
    standardInput = System.in
}